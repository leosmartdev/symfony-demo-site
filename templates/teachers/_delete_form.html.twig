<form method="post" action="{{ path('teachers_detail_delete', {'id': maestros.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maestros.id) }}">
    <button class="btn btn-warning">Delete</button>
</form>
